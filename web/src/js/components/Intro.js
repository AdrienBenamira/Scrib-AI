import React, {Component} from 'react';

export const Intro = (props) => (
        <div>

            <h1>An IA for automatic and abstractive summarization</h1>
            <img src={require('../../images/LogoCS.png')} style={{display: 'block', width: 400,  margin: "0 auto"}} />
            <p>Description</p>


        </div>
);
